{
  "name": "forgeos-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for ForgeOS — OSKernel, AgentTreasury, ForgeOSRegistry on Ethereum Sepolia",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy forgeos",
    "create:local": "graph create --node http://localhost:8020/ forgeos",
    "remove:local": "graph remove --node http://localhost:8020/ forgeos",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 forgeos"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.57.0"
  }
}
